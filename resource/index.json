{
    "generator": {
        "uploadImageLambdaName": "${self:custom.stackPrefix}-upload-image",
        "originSourceBucketName":  "${self:custom.stackPrefix}-origin-source",
        "resizedSourceBucketName":  "${self:custom.stackPrefix}-resized-source"
    }
}